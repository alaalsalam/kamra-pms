# Test Scenarios

_BildFast starter scenarios, derived from the business analysis. **User-owned**: the tester proposes
scenario changes for your approval before saving, and you can **import** your own .md from the Tests tab.
The tester turns approved scenarios into real automated tests and records results below; the **Go-Live**
reviewer runs the suite._

## Scenarios
_For each feature / user journey: the happy path + key edge & error cases (what to verify)._

- _(to be filled — e.g. "Create record: required fields enforced; duplicate name rejected; non-permitted
  role gets 403")_

## Latest test run
_Filled by the tester after running `bench run-tests` — date, command, passed/failed, known gaps._
