# Architecture

_Written in the **Plan** phase from the business analysis. Defines the data model, screens, and the API
contract that the Frontend (mock) and Backend (real) phases both follow._

## Data model
_Entities (doctypes) and their fields + relationships._

## Screen / route map
_Each page, its route, and which features/data it shows._

## API contract
_Each endpoint the UI needs: name, inputs, and response shape. The Frontend phase mocks these in
localStorage with the SAME names/shapes; the Backend phase implements them for real._

## Hypothesis
_The core approach in one or two sentences — what this design is betting on._

## Success criteria
_How we'll know the build is right: observable outcomes to check against._

## Key decisions
_Each as: decision · rationale · trade-off._
